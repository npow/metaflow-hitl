[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "metaflow-hitl"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["metaflow", "requests", "apprise"]

[tool.setuptools.packages.find]
include = ["metaflow_extensions.*"]
